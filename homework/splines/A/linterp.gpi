set terminal png
set output "linterp.png"
set key below
set xlabel "$x$"
set title "Linear interpolation"
plot \
	"data.txt" index 0 using 1:2 with points col black title "$f(x) = \sin(x)$",\
	"data.txt" index 0 using 1:5 with points col grey title "$\int f(x) = -\cos(x)$",\
	"data.txt" index 0 using 1:3 with lines col red title "Linear interpolation",\
	"data.txt" index 0 using 1:4 with lines col blue title "Linear interpolation integral"
